//no cambiaré la estructura porque se nos la dieron así el enunciado del problema
function canPlay() {
    let sport = " Football";
    
    if (true) {
    var personName = "Cosimo"; //cambio LET por VAR
    }
    
    console.log(personName + sport);
    }
    
    canPlay();

//MI EXPLICACIÓN:
//si utilizo una variable de tipo LET dentro de un bloque IF
//esta variable será local y no podré utilizarla afuera de
//las llaves en donde está metida. Una de las formas de hacer
//que el codigo funcione es utilizar una variable del tipo VAR
//porque la podré acceder afuera de su bloque.