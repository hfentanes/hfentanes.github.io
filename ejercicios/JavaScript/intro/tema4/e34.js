//declaro mis variables (me gusta declarar variables aunque no esté obligada a hacerlo)
let x;
let y;

//creo la función de flecha (he elegido X e Y porque suelen aparecer como variables de ecuaciones matemáticas)
let sum = (x,y) => x + y;

//sale en pantalla (puse dos 5 para que me saliera el mismo resultado del ejercicio 33)
console.log(sum(5,5));
