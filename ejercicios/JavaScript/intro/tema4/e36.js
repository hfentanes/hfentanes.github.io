//no cambiaré la estructura porque se nos la dieron así el enunciado del problema
//simplemente añadiré una variable que sea global y comentaré la línea en que aparecía antes
let sport = " Football";

function canPlay() {
    /* let sport = " Football"; */
    
    if (true) {
    var personName = "Cosimo"; //vuelvo a cambiar LET por VAR
    }
    
    console.log(personName + sport);
    }
    
    canPlay();