//declaro las variables (con CONST porque un nombre, en general, no se cambia)
const firstName = "Hadassa";
const lastName = "Fentanes";

//uno las dos variables en una
const fullName = firstName + " " + lastName;

//lo que sale en pantalla
console.log("Mi nombre es " + fullName + ".")