//datos del enunciado
const person = {
  id: 1,
  firstName: "Mario",
  lastName: "Rossi",
  age: 25,
};

//asigno los valores en una línea
const { id, firstName, lastName, age } = person;

//sale en pantalla
console.log(id, firstName, lastName, age);
